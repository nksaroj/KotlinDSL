ext {
    //Application
    appVersionName = "1.0"
    appVersionCode = 1
    minSdkVersion = 15
    compileSdkVersion = 28
    targetSdkVersion = 28
    buildToolsVersion = "28.0.1"

    //Android
    androidSupportVersion = "1.0.0-rc01"
    constraintLayoutVersion = "2.0.0-alpha2"

    //GSON, Retrofit and etc.

    //Testing
    junitVersion = "4.12"
    junitTestRunnerVesion = "1.1.0-alpha4"
    espressoCoreVersion = "3.1.0-alpha4"

    androidLibraries = [
            appcompat       : "androidx.appcompat:appcompat:${androidSupportVersion}",
            constraintlayout: "androidx.constraintlayout:constraintlayout:${constraintLayoutVersion}",
    ]

    libraries = [
            //Add here
    ]

    testLibraries = [
            junit       : "junit:junit:${junitVersion}",
            testRunner  : "androidx.test:runner:${junitTestRunnerVesion}",
            espressoCore: "androidx.test.espresso:espresso-core:${espressoCoreVersion}",
    ]
}
